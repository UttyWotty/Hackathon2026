# ==========================================
# Manufacturing Analytics MCP Server Suite
# Complete Requirements - All Phases
# ==========================================

# FastAPI and Core
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6

# MCP Framework - Model Context Protocol
mcp>=1.0.0
# Note: If mcp package not available, use alternative:
# git+https://github.com/anthropics/mcp-python.git

# Database
sqlalchemy>=2.0.0  # ORM for SQLite and database models
snowflake-connector-python>=3.0.0
snowflake-snowpark-python>=1.0.0

# Data Processing
pandas>=2.0.0
numpy>=2.0.0
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# AI/ML
langchain>=0.1.0
langchain-aws>=0.1.0
boto3>=1.28.0

# Machine Learning (Phase 4)
scikit-learn>=1.3.0  # Anomaly detection, forecasting, predictions

# Visualization
plotly>=5.14.0
matplotlib>=3.7.0
seaborn>=0.12.0
kaleido>=0.2.1  # For plotly static image export
streamlit>=1.28.0  # For interactive dashboards

# Reporting
reportlab>=4.0.0
weasyprint>=60.0  # HTML to PDF conversion
cairocffi>=1.6.0  # Required by weasyprint
pillow>=10.0.0  # Image processing for reports

# Utilities
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Authentication & Security (Phase 1)
pyjwt>=2.8.0  # JWT token generation and validation
cryptography>=41.0.0  # For secure key handling

# Async & File Operations
aiofiles>=23.0.0
aiohttp>=3.9.0
httpx>=0.25.0

# Scheduling
APScheduler>=3.10.0
croniter>=1.4.1  # Cron expression parsing for scheduler

# Caching & Token Storage (Phase 1)
redis>=5.0.0  # Redis client (optional, falls back to in-memory)

# Email
aiosmtplib>=3.0.0  # Async SMTP for email sending

# Logging
loguru>=0.7.0  # Better logging

# Security & Validation
email-validator>=2.1.0

# Process Management
psutil>=5.9.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# Development Tools (optional)
black>=23.0.0  # Code formatting
isort>=5.12.0  # Import sorting
ruff>=0.1.0    # Fast linter
mypy>=1.7.0    # Type checking

# ==========================================
# Phase-Specific Requirements Summary:
# 
# Phase 1 (Auth & Audit):
#   - pyjwt, cryptography, redis
# 
# Phase 2 (Monitoring & Alerts):
#   - (uses existing dependencies)
# 
# Phase 3 (Data Transformation):
#   - (uses existing pandas, numpy)
# 
# Phase 4 (Model / AI):
#   - scikit-learn
# 
# Phase 5 (Backup & Recovery):
#   - (uses existing dependencies)
# ==========================================

# ==========================================
# Installation Instructions:
# 
# 1. Create virtual environment:
#    python -m venv venv
#    source venv/bin/activate  # On Windows: venv\Scripts\activate
#
# 2. Install all dependencies:
#    pip install -r config/requirements.txt
#
# 3. For development:
#    pip install -r config/requirements.txt --upgrade
#
# 4. Verify installation:
#    python scripts/verify_env.py
# ==========================================

# ==========================================
# Optional Dependencies:
# 
# Redis (recommended for production):
#   - Provides persistent token storage
#   - Improves cache performance
#   - Falls back to in-memory if not available
#   
#   Install Redis:
#   - macOS: brew install redis
#   - Ubuntu: sudo apt install redis-server
#   - Docker: docker run -d -p 6379:6379 redis
#
# FAISS (recommended for Vector Search MCP):
#   - Provides fast similarity search
#   - Significantly faster than numpy fallback
#   - Falls back to numpy-based search if not available
#
#   Install FAISS:
#   - CPU version: pip install faiss-cpu
#   - GPU version: pip install faiss-gpu (requires CUDA)
# ==========================================

