# Autonomous Manufacturing Workflow Agent - Environment Configuration
# Copy this file to .env and fill in your actual values.
# .env is gitignored and must never be committed.

# ============================================================================
# SNOWFLAKE DATABASE (Required)
# ============================================================================
SNOWFLAKE_ACCOUNT=your_org-your_account
SNOWFLAKE_USER=your_username
SNOWFLAKE_PASSWORD=your_password
SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_DATABASE=MMS_DEMO
SNOWFLAKE_SCHEMA=PUBLIC
SNOWFLAKE_ROLE=ACCOUNTADMIN

# ============================================================================
# LLM BACKEND
# ============================================================================
# cortex = Snowflake Cortex Messages API (Claude)  [default, submission path]
# mlx    = local mlx_lm.server on Apple Silicon    [development only]
LLM_BACKEND=cortex

# ============================================================================
# SNOWFLAKE CORTEX (Required when LLM_BACKEND=cortex)
# ============================================================================
# Programmatic Access Token for Cortex REST API authentication.
# Create via Snowsight: Admin > Security > Programmatic Access Tokens
SNOWFLAKE_PAT=your_programmatic_access_token

# Model id. Confirm available models with: SHOW CORTEX BASE MODELS;
CORTEX_MODEL=claude-sonnet-4-5

# Per-request timeout in seconds.
CORTEX_TIMEOUT_SECONDS=60

# Prompt caching (recommended: saves credits on repeated tool schemas).
CORTEX_ENABLE_PROMPT_CACHING=true

# ============================================================================
# LOCAL DATA SOURCE (Optional - run without Snowflake)
# ============================================================================
# When set, analysis modules read from synthetic CSV instead of Snowflake.
# Leave empty to query the live database.
#
# Generate the dataset:
#   python -m synthetic_data.generate --output-dir ./synthetic_out
#
# Then set:
#   LOCAL_DATA_DIR=./synthetic_out
LOCAL_DATA_DIR=

# ============================================================================
# SERVER (Optional - only needed if running the FastAPI server)
# ============================================================================
SERVER_PORT=3020
SERVER_HOST=0.0.0.0

# ============================================================================
# MLX LOCAL LLM (Optional - Apple Silicon development only)
# ============================================================================
# Start the server:
#   mlx_lm.server --model mlx-community/Qwen3-32B-4bit --port 8080
MLX_HOST=http://127.0.0.1:8080
MLX_LLM_MODEL=mlx-community/Qwen3-32B-4bit
