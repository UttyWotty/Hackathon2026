# Manufacturing Analytics API - Complete Dependencies
# ===================================================

# Core Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6

# Database & ORM
sqlalchemy>=2.0.0          # ORM for SQLite
alembic>=1.12.0            # Database migrations (optional)

# Email (Production-Ready)
jinja2>=3.1.0              # HTML email templates

# Snowflake
snowflake-connector-python>=3.0.0
snowflake-snowpark-python>=1.0.0
requests>=2.31.0           # Cortex Messages REST calls (core/cortex_client.py)

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
xlsxwriter>=3.1.0
python-pptx>=0.6.21            # PowerPoint generation

# Caching
redis>=5.0.0               # Redis client (optional, graceful fallback)

# Scheduler
croniter>=2.0.0            # Cron expression parser (for job scheduling)
tenacity>=8.2.0            # Retry logic with exponential backoff

# System Monitoring
psutil>=5.9.0              # System metrics

# Visualization (optional)
plotly>=5.14.0             # Interactive charts
kaleido>=0.2.1             # For plotly static image export
matplotlib>=3.7.0          # Static charts (RCA visualizations, reporting)
seaborn>=0.12.0            # Statistical plots (RCA visualizations)

# Scientific computing
scipy>=1.10.0              # Statistics (tooling EOL, capacity analysis)
scikit-learn>=1.3.0        # Regression/clustering (forecasting, benchmarking)

# Email
# (uses built-in smtplib, no extra dependencies needed)

# AI/ML
mlx-lm>=0.20.0             # Local LLM inference (MLX on Apple Silicon)
sentence-transformers>=2.0.0  # Text embeddings (nomic-embed-text)
soxr>=1.0.0                # Required by transformers lazy Trainer import (audio_utils)
boto3>=1.28.0              # AWS SDK (kept for potential future use)

# Observability
langfuse>=2.0.0            # LLM tracing and evaluation (self-hosted)

# Utilities
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0

# Async Operations
aiofiles>=23.0.0
httpx>=0.25.0

# Logging
# (uses built-in logging, no extra dependencies needed)

# Security & Validation
email-validator>=2.1.0

# Demo UI
streamlit>=1.40.0          # demo/app.py only; the API never imports it

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Development Tools (optional)
black>=23.0.0              # Code formatting
isort>=5.12.0              # Import sorting
ruff>=0.1.0                # Fast linter
