"""
Streamlit demo surface for the autonomous workflow agent.

Holds the demo application only: a trigger for a live headless run, a decision
trail viewer, and the drift chart that shows what the agent has to catch. No
module outside this package imports it, so the demo can never affect the API.
"""
